<body
  class='bg-white dark:bg-slate-800 transition-colors font-sans antialiased min-h-screen'
>
  <!-- <div class='flex'> -->
  <div
    class='transition-colors border-18 border-theme-primary dark:border-theme-dark-secondary card min-h-screen dark:bg-slate-800'
  >
    <main class='grid gap-8 mx-auto max-w-5xl px-4 md:px-0'>
      <slot />
    </main>
  </div>
  <!-- </div> -->
</body>

<style>
  @property --gradient-angle {
    syntax: "<angle>";
    initial-value: 0deg;
    inherits: false;
  }

  .card {
    border: 18px solid;
    border-color: #4e00c2;
    --angle: 0deg;
    position: relative;
    background: white;

    border-image: linear-gradient(
        var(--gradient-angle),
        #5ddcff,
        #3c67e3 43%,
        #4e00c2
      )
      1;
    animation: rotation 20s linear infinite;
  }

  /* .card::before,
  .card::after {
    content: "";
    position: absolute;
    inset: 0;
    z-index: -1;
    background: conic-gradient(
      from var(--gradient-angle),
      #5ddcff,
      #3c67e3 43%,
      #4e00c2
    );
    border-radius: inherit;
    animation: rotation 20s linear infinite;
  } */

  /* .card::after {
    filter: blur(3.5rem);
  } */

  @keyframes rotation {
    0% {
      --gradient-angle: 0deg;
    }
    100% {
      --gradient-angle: 360deg;
    }
  }
</style>
